# Релиз 15.08.2026

База: архив с хостинга `15.08.26.zip`, скачан 15.08.2026.
Папка `Harmony Dental Clinic/` приведена к состоянию хостинга + правки ниже.
Июньская версия папки убрана в `_work/backups/2026-08-15_Harmony-Dental-Clinic-июньская-версия/`.

## Что вошло — 34 файла

1. **implants-ua.html — заменён на доработанную версию** (488 -> 2896 слов, 19 H2).
   Дополнительно: «Одеса» 9 -> 13, «ціна» 1 -> 5, добавлен H2
   «Ціни на імплантацію зубів в Одесі — вартість під ключ»,
   заголовок «Що відбувається, якщо вчасно не вставити зуб» (кластер ядра не был покрыт).

2. **ru/implants-ua.html — собрана русская версия** (488 -> 2907 слов, 19 H2).
   Разметка украинской страницы сохранена байт в байт, переведены только 318 текстовых
   узлов и alt/aria-label. Хедер и футер взяты из действующей русской версии.
   Локализовано: lang, title, description, og/twitter, canonical на /ru/, hreflang,
   JSON-LD (inLanguage ru-UA, адрес и районы по-русски, @id на /ru/),
   ссылка на врача -> maliukin-andrey.html.

3. `<meta charset="UTF-8">` перенесён в начало `<head>` — 32 файла (было на 2759-м байте).
4. `width`/`height` — 254 изображениям, размеры прочитаны из самих файлов.
5. `loading="lazy"` + `decoding="async"` — 44 изображения ниже первого экрана.
   Логотипы и первый экран намеренно не ленивые. `fetchpriority="high"` — только hero.
6. `<picture>` + WebP — 60 изображений переведены на готовые .webp-дубликаты.
7. `aggregateRating` и `review` удалены из JSON-LD — 9 узлов на 6 страницах.
8. `<meta name="keywords">` удалён — 8 файлов.
9. 404.html и ru/404.html — добавлены description, canonical, robots noindex,follow.
10. sitemap.xml пересобран: hreflang у всех 30 URL (было у трёх), lastmod по факту.
11. Из папки убраны 11 файлов `.fuse_hidden*` — **на сервере их надо удалить отдельно**,
    заливка папки их оттуда не уберёт.

## Проверено после сборки

- видимый текст совпадает с оригиналом везде, кроме двух страниц имплантации
- весь JSON-LD парсится, теги `<picture>` сбалансированы, битых ссылок нет
- lechenie-ua.html 623 слова, otbilyuvannya-ua.html 1031 слово — продакшен-версии сохранены
- 332 `<img>`, без размеров только 18 пустых заглушек лайтбокса

## Не залито на хостинг

На момент 15.08.2026 релиз на сервер НЕ залит. После заливки:
- обновить дату lastmod в sitemap, если заливка не 15 августа
- отправить sitemap заново в Search Console
- запросить переиндексацию обеих страниц имплантации
- удалить на сервере `/blog/.fuse_hidden*` и `.DS_Store`

## База для сравнения — Search Console, 3 месяца на 15.08.2026

| Страница | Показы | Клики | Позиция |
|---|---|---|---|
| /blog/implantatsiya-zubiv-povnyi-gayd.html | 623 | 0 | 44.5 |
| /implants-ua.html | 207 | 0 | 33.2 |
| /ru/implants-ua.html | 680 | 14 | 41.0 |

Каннибализации нет: пересечение запросов между этими тремя страницами = 0.
Гипотеза августовского аудита о каннибализации проверена и опровергнута.
Статью блога чистить не нужно.
